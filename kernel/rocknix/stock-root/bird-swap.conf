# Fixed 2 GiB H700 memory policy. Keep ROCKNIX's proven one-shot VM setup,
# but do not wake ksmd to deduplicate an idle single-user appliance.
ZRAM_SIZE="0"
SWAP_FILE_SIZE="0"
VM_WATERMARK_FACTOR="10"
VM_COMPACTION="10"
VM_OVERCOMMIT="0"
KSM_ENABLE="disable"
