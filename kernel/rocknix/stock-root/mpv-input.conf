# bird-mpv-controls owns the fixed H700 gamepad through one validated evdev
# descriptor and sends direct JSON IPC commands. The player wrapper disables
# MPV's default and SDL gamepad bindings, eliminating the former dual-reader
# translation path. Dedicated volume keys remain owned by Bird's global worker.
VOLUME_DOWN ignore
VOLUME_UP ignore
