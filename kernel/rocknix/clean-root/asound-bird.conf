# Keep ALSA's complete native type/plugin definitions, then give applications
# that open the generic "default" endpoint Bird's one fixed H616 codec.
</usr/share/alsa/alsa.conf>

pcm.!default {
	type plug
	slave.pcm "hw:0,0"
}

ctl.!default {
	type hw
	card 0
}
